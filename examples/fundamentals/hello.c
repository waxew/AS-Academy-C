/*
 * AS Academy - C Programming
 * مثال پایه: اولین برنامه C.
 * این کامنت ها عمداً آموزشی هستند تا هنرجو نقش هر بخش را درک کند.
 */
#include <stdio.h> /* اعلان printf از کتابخانه استاندارد ورودی/خروجی. */

int main(void) /* نقطه شروع اجرای برنامه؛ void یعنی آرگومان ورودی تعریف نکرده ایم. */
{
    /* متن آموزشی را در خروجی استاندارد چاپ می کنیم. */
    printf("Hello from AS Academy C!\n");

    /* صفر به محیط اجرا اعلام می کند برنامه با موفقیت پایان یافته است. */
    return 0;
}
